version: '3.8'

services:
  sandbox:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - LLM_BACKEND=dummy
      - DUCKDB_PATH=simulation_runs.duckdb
    restart: always
